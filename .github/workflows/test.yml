name: test
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    container: ros:melodic-ros-core-bionic
    name: test
    steps:
      - uses: actions/checkout@v3
      - run: |
         catkin test

